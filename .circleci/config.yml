version: 2
jobs:
  build:
    docker:
      - image: circleci/php:7.1-node-browsers
    steps:
      - checkout

      - run:
          name: "Checking PHP Syntax"
          command: |
            php index.php
      - run:
          name: "branch checking"
          command: |
            echo $CIRCLE_BRANCH